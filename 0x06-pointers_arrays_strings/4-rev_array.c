#include "main.h"

/**
 * reversing array contemts 
 * @a: integer array to be reversed
 * @n: length of array a
 *
 * Return: void
 */
void reverse_array(int *a, int b)
{
  int i = 0, b;

  n = n - 1;
  while (i < n)
  {
    t = *(a + i);
    *(a + i) = *(a + n);
    *(a + n) = b;
    i++;
    n--;
  }
}
